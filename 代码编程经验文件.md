# 代码编程经验文件

目的：记录本项目里出现过的低级错误和正确做法，避免重复踩坑。

## 使用规则

- 每次出现低级错误，必须新增一条记录。
- 记录要包含：错误现象、根因、正确做法、预防检查。
- 修复完成后再补一行“验证结果”。

## 记录模板

### [日期] 标题

- 错误现象：
- 根因：
- 正确做法：
- 预防检查（下次提交前）：
- 验证结果：

---

## 已记录经验

### [2026-04-07] XPI 打包后插件导入不可见

- 错误现象：生成的 xpi 在 Zotero 中导入后看不到插件。
- 根因：在 Windows 下使用 `Compress-Archive` 直接打包，压缩包内部条目路径可能变成反斜杠（如 `chrome\content\...`），导致插件资源加载失败。
- 正确做法：使用 .NET `System.IO.Compression.ZipArchive` 手动写入，并强制将路径分隔符转为 `/`。
- 预防检查（下次提交前）：
  - 检查 xpi 内部条目是否都是 `/`。
  - 检查包根目录是否直接包含 `manifest.json`、`bootstrap.js`、`prefs.js`、`chrome/...`。
- 验证结果：已生成 `rss-daily-translator-fixed.xpi`，`BAD_BACKSLASH_ENTRIES=0`。

### [2026-04-07] 依赖 manifest 解析导致打包脚本失败

- 错误现象：脚本中 `ConvertFrom-Json` 解析 `manifest.json` 失败，打包中断。
- 根因：脚本把“读取版本号”作为前置硬依赖，一旦 JSON 格式或编码异常就无法继续打包。
- 正确做法：打包流程与版本读取解耦；即使读取版本失败，也要能产出固定文件名的可安装包。
- 预防检查（下次提交前）：
  - 先做 JSON 有效性检查，再读取版本号。
  - 提供兜底输出名（如 `rss-daily-translator-fixed.xpi`）。
- 验证结果：已采用不依赖版本号的兜底打包流程。

### [2026-04-07] 设置按钮缺少用户可见反馈

- 错误现象：点击“保存设置/立即运行”后无提示，用户不知道是否成功。
- 根因：只有日志输出，没有界面反馈组件。
- 正确做法：在设置页增加反馈标签，并在按钮处理函数中统一显示成功/失败/进行中状态。
- 预防检查（下次提交前）：
  - 每个按钮都要有“成功/失败”可见反馈。
  - 异步操作必须先提示“处理中”，结束后提示结果。
- 验证结果：已在偏好页新增反馈文案逻辑。

### [2026-04-07] XPI 文件名必须沿用既有版本格式

- 错误现象：打包时输出了不规范文件名（如 `rss-daily-translator-fixed.xpi`），与既有发布命名不一致。
- 根因：为了兜底打包临时改用固定名称，没有优先沿用项目历史版本命名规范。
- 正确做法：始终按 `rss-daily-translator-<manifest.version>.xpi` 命名，例如 `rss-daily-translator-1.0.3.xpi`。
- 预防检查（下次提交前）：
  - 先读取 `addon/manifest.json` 的 `version`。
  - 打包输出文件名必须匹配 `rss-daily-translator-版本号.xpi`。
  - 只在“明确失败兜底”场景使用临时名，最终交付前必须改回规范名。
- 验证结果：后续打包统一使用历史命名格式，不再使用随意名称。
- 不要覆盖之前的XPI，每次完善一个功能，就新生成一个新的XPI文件

### [2026-04-07] 勾选“创建分类子集合”但未实际生效

- 错误现象：设置中勾选“创建分类子集合”后，点击“立即运行”仍只写入父集合，看不到任何分类子集合。
- 根因：代码只读取了 `createCategorySubCollection` 偏好项，但未在入库逻辑里根据分类创建子集合并挂载条目。
- 正确做法：在创建条目时按开关分流：
  - 先确保父集合存在；
  - 开关为 true 时，按 RSS `category` 或 feed URL 推导子集合名；
  - 在父集合下创建/复用子集合，再将条目写入该子集合。
- 预防检查（下次提交前）：
  - 每个设置项都要有“读取 + 实际行为”闭环检查；
  - 手工验证：勾选和不勾选两种路径都要跑一次“立即运行”；
  - 确认条目最终落在预期集合层级。
- 验证结果：已实现子集合创建逻辑并随版本 `1.0.5` 打包发布。

### [2026-04-07] 需求优先级错误：用户要单集合，不要分类

- 错误现象：用户只想所有论文进一个集合，但实现中增加了分类子集合，造成理解与使用负担。
- 根因：过度实现可选功能，没有优先收敛到用户当前主需求（单集合可见、可验证）。
- 正确做法：默认并优先支持“单集合模式”，复杂分流功能只有在用户明确持续需要时再保留。
- 预防检查（下次提交前）：
  - 先复述并确认核心目标是否是“单路径最简可用”；
  - 新增功能前检查是否会增加排查复杂度。
- 验证结果：已移除“创建分类子集合”设置，统一写入目标父集合。

### [2026-04-07] 运行统计口径必须与真实结果一致

- 错误现象：界面显示“新增 50 条”，但文库中看不到对应新增，用户误以为写入失败。
- 根因：代码把“已存在条目”也计入 `itemsCreated`，统计口径与实际新增不一致。
- 正确做法：拆分统计字段：`itemsCreated`（真正新建）与 `itemsExisting`（已存在但已归入集合）。
- 预防检查（下次提交前）：
  - 检查每个统计字段是否有唯一且准确的语义；
  - 抽样核对 summary 与文库真实增量是否一致。
- 验证结果：已修正统计逻辑，并增加可读摘要文案。

### [2026-04-07] Zotero 条目字段与集合 API 使用错误会导致全量失败

- 错误现象：运行结果出现“抓取很多、新增 0、错误全量、全部入重试队列”。
- 根因：
  - 使用了不存在或不稳定的字段方法（如 `setTitle` / `setAbstract`）；
  - 集合关联时使用了 `collection.key`，而 `addToCollection` 需要集合 ID。
- 正确做法：
  - 使用 `item.setField("title", value)` 与 `item.setField("abstractNote", value)`；
  - 集合关联统一使用 `collection.id`（或 `collection.collectionID`）并在变更后保存。
- 预防检查（下次提交前）：
  - 先验证每个 API 是否为当前 Zotero 版本可用；
  - 对“单条写入”做冒烟测试，确认不会出现全量失败再跑批量。
- 验证结果：已在版本 `1.0.7` 修复并重新打包。

### [2026-04-07] 先看统计字段再定位阶段：抓取成功 != 入库成功

- 错误现象：`抓取条目` 很高但 `新增条目` 为 0，`错误数` 接近抓取条目数。
- 根因：误以为是 RSS 解析失败；实际是入库阶段字段写入（DOI/URL）校验导致批量异常。
- 正确做法：先用统计判断阶段：
  - `抓取条目 > 0` 说明 RSS 请求和解析基本正常；
  - `新增条目 = 0 且 错误高` 优先排查 `createZoteroItem` 和字段赋值。
- 预防检查（下次提交前）：
  - 对 DOI 做规范化（去 `doi.org/` 前缀、去 query/hash）；
  - DOI/URL 字段赋值失败必须降级为跳过，不阻断整条入库。
- 验证结果：已在版本 `1.0.8` 增加 DOI/URL 容错与错误示例展示。

### [2026-04-07] collectionItems 外键报错通常是跨文库（libraryID）写入

- 错误现象：`collectionItems` 插入失败，提示 `violates foreign key constraint ... libraryID`。
- 根因：条目和目标集合不在同一 `libraryID`（例如去重返回了其他文库条目，或新建条目未绑定目标文库）。
- 正确做法：
  - 新建条目前显式设置 `item.libraryID = targetCollection.libraryID`；
  - 去重查询仅在目标文库中匹配，避免跨库复用已有条目。
- 预防检查（下次提交前）：
  - 关联集合前检查 item/library 与 collection/library 是否一致；
  - 发现外键错误时优先排查 libraryID，不要先怀疑 RSS 解析。
- 验证结果：已在版本 `1.0.9` 修复并打包。

### [2026-04-07] Zotero API 在不同环境可能缺失（如 getByDOI）

- 错误现象：批量运行时报 `Zotero.Items.getByDOI is not a function`，导致大量条目进入重试队列。
- 根因：直接调用可选 API，没有先做函数存在性判断。
- 正确做法：
  - 调用前先判断 `typeof Zotero.Items.getByDOI === "function"`；
  - 若不存在，回退到 `Zotero.Search` 按 `DOI` / `title` 查询。
- 预防检查（下次提交前）：
  - 对 Zotero 非核心 API 统一加能力探测；
  - 任何查重逻辑都要有回退路径，不能因单个 API 缺失阻断整批处理。
- 验证结果：已在版本 `1.0.10` 修复并打包。

### [2026-04-08] 已存在条目被直接跳过会造成“翻译没运行”的错觉

- 错误现象：运行显示“新增 0、已存在 50、错误 0”，用户认为翻译功能没有执行。
- 根因：`createZoteroItem` 在命中已存在条目后直接返回，未对缺失字段（如摘要）执行补写与翻译。
- 正确做法：已存在条目也要做“补全更新”流程：缺 `abstractNote` 时翻译并写入；缺 DOI/URL 时补写；仅在有变更时保存。
- 预防检查（下次提交前）：
  - Summary 中单独统计 `更新条目`，避免“新增=0”被误解为“什么都没做”；
  - 回归测试要覆盖“全已存在但字段缺失”的场景。
- 验证结果：已在版本 `1.0.15` 实现 `itemsUpdated` 统计并打包。

### [2026-04-08] 按标题全库去重会误判“已存在”，导致新条目不翻译

- 错误现象：抓取很多（如 223），但显示“新增 0、更新 0、错误 0”，运行秒级结束，看起来像翻译没执行。
- 根因：标题去重在整个文库范围匹配，同名条目（不在目标集合）也被当作已存在，导致创建与翻译都被跳过。
- 正确做法：DOI 仍可全库去重；标题去重必须限制在目标集合内，避免跨集合同名误判。
- 预防检查（下次提交前）：
  - 用“目标集合外同名条目”做回归，确认不会被误判已存在；
  - 观察运行耗时与 `更新条目`，若秒级结束且全 0 要优先排查去重范围。
- 验证结果：已在版本 `1.0.17` 将标题去重限制到目标集合并打包。

### [2026-04-08] 标题翻译应优先写 ExtraField 的 titleTranslation

- 错误现象：用户在信息面板“标题翻译”框看不到结果，反而看到短标题残留值。
- 根因：仅依赖 `item.setField("titleTranslation", ...)` 在不同环境可用性不稳定，且历史数据可能仍在 `shortTitle`。
- 正确做法：按扩展字段思路写入 `extra` 中的 `titleTranslation: xxx`，并同步尝试标准字段；写入成功后清理旧 `shortTitle` 遗留。
- 预防检查（下次提交前）：
  - 检查 `extra` 是否包含 `titleTranslation:` 行；
  - 确认 `shortTitle` 未被当作翻译结果存储。
- 验证结果：已在版本 `1.0.20` 切换为 ExtraField 兼容写入并打包。

### [2026-04-08] XPI 打包时不要把 `ConvertFrom-Json` 当硬依赖

- 错误现象：打包命令在读取 `manifest.json` 时 `ConvertFrom-Json` 失败，导致无法产出新 XPI。
- 根因：将“解析完整 JSON”作为获取版本号的唯一前置步骤，容错性不足。
- 正确做法：打包主流程与 JSON 解析解耦；版本号优先用正则提取 `"version"`，保证打包可继续执行。
- 预防检查（下次提交前）：
  - 即使 JSON 解析异常，也要能继续生成版本化 XPI；
  - 打包后必须检查 `BAD_BACKSLASH_ENTRIES=0` 与关键文件齐全。
- 验证结果：已成功生成 `rss-daily-translator-1.0.21.xpi` 并通过结构校验。

### [2026-04-09] 需求已定时不要保留多余兼容分支

- 错误现象：用户已明确要求父集合下直接使用时间戳批次集合（如 `20260409-0930`），代码仍保留旧的 `YYYY-MM-DD` / `run-xxxxxx` 命名兼容分支。
- 根因：实现时习惯性保留历史兼容，未严格按当前需求收敛，增加了清理规则复杂度。
- 正确做法：当用户明确“不需要兼容旧结构”时，集合识别与清理规则只保留当前命名规范（`YYYYMMDD-HHMM`），删除冗余分支。
- 预防检查（下次提交前）：
  - 逐条核对“需求必须项/不需要项”，确认未额外加入兼容逻辑；
  - 检查集合命名匹配规则是否只有一个规范入口；
  - 检查清理逻辑是否仅作用于当前规范生成的集合。
- 验证结果：已将命名识别收敛为 `^\\d{8}-\\d{4}$`，并移除旧命名兼容。
